#
#	Makefile for oflog/docs
#

@SET_MAKE@

SHELL = /bin/sh
srcdir = @srcdir@
top_srcdir = @top_srcdir@
configdir = @top_srcdir@/@configdir@

include $(configdir)/@common_makefile@


all:

install:

clean:
	rm -f $(TRASH)

distclean:
	rm -f $(DISTTRASH)
