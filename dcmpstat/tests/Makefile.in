#
#	Makefile for dcmnet tests
#


@SET_MAKE@

SHELL = /bin/sh
VPATH= @srcdir@:@top_srcdir@/include:@top_srcdir@/@configdir@/include
srcdir= @srcdir@
top_srcdir= @top_srcdir@
configdir=@top_srcdir@/@configdir@

include $(configdir)/@common_makefile@

ofstddir=$(top_srcdir)/../ofstd
dcmdatadir=$(top_srcdir)/../dcmdata
dcmnetdir=$(top_srcdir)/../dcmnet
imagectndir=$(top_srcdir)/../imagectn
dcmimgledir=$(top_srcdir)/../dcmimgle

LOCALINCLUDES=-I$(dcmnetdir)/include -I$(dcmdatadir)/include \
	-I$(ofstddir)/include -I$(ofstddir)/include \
	-I$(imagectndir)/include -I$(dcmimgledir)/include


LIBDIRS = -L$(top_srcdir)/libsrc -L$(dcmnetdir)/libsrc -L$(ofstddir)/libsrc \
	-L$(dcmdatadir)/libsrc -L$(dcmimgledir)/libsrc -L$(libdir)\
	-L$(imagectndir)/libsrc 

LOCALLIBS = -ldcmpstat -ldcmimgle -limagedb  -ldcmnet -ldcmdata -lofstd 


objs = test.0

progs= test


all: $(progs)

  
test: test.o
	$(CXX) $(CXXFLAGS) $(LDFLAGS) $(LIBDIRS) -o $@ test.o $(LOCALLIBS) $(LIBS)  

install: all
	$(configdir)/mkinstalldirs $(bindir)
	for prog in $(progs); do \
		$(INSTALL_PROGRAM) $$prog $(bindir); strip $(bindir)/$$prog ;\
	done

clean:
	rm -f $(objs) $(progs) $(TRASH)

distclean: clean
	rm -f $(DISTTRASH)


dependencies:
	$(CXX) -MM $(defines) $(includes) $(CPPFLAGS) $(CXXFLAGS) *.cc  > $(DEP)

$(objs): config.h


include $(DEP)





