#
#	Makefile for dcmnet tests
#


@SET_MAKE@

SHELL = /bin/sh
VPATH= @srcdir@:@srcdir@/../include:@top_srcdir@/@configdir@/include
srcdir= @srcdir@
top_srcdir= @top_srcdir@
configdir=@top_srcdir@/@configdir@

include $(configdir)/@common_makefile@


dcmdatadir=$(top_srcdir)/../dcmdata

LOCALINCLUDES=-I$(dcmdatadir)/include -I$(includedir)/dcmdata

LIBDIRS = -L$(top_srcdir)/libsrc -L$(dcmdatadir)/libsrc -L$(libdir)
LOCALLIBS = -ldcmnet -ldcmdata


objs= 

progs= 


all: $(progs)


install: 
	echo Nothing to for tests install


clean:
	rm -f $(objs) $(progs) $(TRASH)

distclean: clean
	rm -f $(DISTTRASH)


dependencies:
	touch $(DEP)
#	$(CXX) -MM $(defines) $(includes) $(CPPFLAGS) $(CXXFLAGS) *.cc  > $(DEP)

$(objs): config.h


include $(DEP)





