#
#	Makefile for dcmtls/libsrc
#


@SET_MAKE@

SHELL = /bin/sh
VPATH=@srcdir@:@top_srcdir@/include:@top_srcdir@/@configdir@/include
srcdir=@srcdir@
top_srcdir=@top_srcdir@
configdir=@top_srcdir@/@configdir@

dcmdatadir=$(top_srcdir)/../dcmdata
ofstddir=$(top_srcdir)/../ofstd
dcmimgledir=$(top_srcdir)/../dcmimgle
imagectndir=$(top_srcdir)/../imagectn
dcmnetdir=$(top_srcdir)/../dcmnet

LOCALINCLUDES=-I$(ofstddir)/include -I$(dcmdatadir)/include -I$(dcmimgledir)/include \
  -I$(imagectndir)/include -I$(dcmnetdir)/include \
  -I$(includedir)/ofstd -I$(includedir)/dcmdata -I$(includedir)/dcmimgle \
  -I$(includedir)/imagectn -I$(includedir)/dcmnet


LOCALDEFS=

include $(configdir)/@common_makefile@

objs =  tlstrans.o tlslayer.o

all: libdcmtls.$(LIBEXT)


install: all
	$(configdir)/mkinstalldirs $(libdir)
	$(INSTALL_DATA) libdcmtls.$(LIBEXT) $(libdir)/libdcmtls.$(LIBEXT)


clean:
	rm -f $(objs)
	rm -f libdcmtls.$(LIBEXT)
	rm -f $(TRASH)

distclean: clean
	rm -f $(DISTTRASH)


libdcmtls.$(LIBEXT): $(objs)
	$(AR) $(ARFLAGS) $@ $(objs)
	$(RANLIB) $@


# Dependencies

dependencies:
	$(CXX) -MM $(defines) $(includes) $(CPPFLAGS) $(CXXFLAGS) *.cc  > $(DEP)

include $(DEP)
