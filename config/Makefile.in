#
#	Makefile for config
#


@SET_MAKE@

SHELL = /bin/sh

include @common_makefile@


all:
	echo Nothing to do for all

install:

install-bin:

install-lib: include-install

install-include: include-install

install-support:

libsrc-all:


include-install:
	./mkinstalldirs $(includedir)
	$(INSTALL_DATA) include/osconfig.h $(includedir)
	$(INSTALL_DATA) include/cfunix.h $(includedir)

clean:
	(cd include; rm -f $(TRASH))
	rm -f $(TRASH)

distclean:
	(cd include; rm -f $(DISTTRASH) cfunix.h)
	rm -f $(DISTTRASH) Makefile.def


dependencies:
	echo Nothing to do for dependencies



